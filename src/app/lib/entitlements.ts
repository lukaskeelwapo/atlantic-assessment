// An entitlement is a particular access rule. This is what the front end checks in order to limit/provide access
export enum Entitlements {
  READ_CONTENT = "READ_CONTENT",
  PRINT_MAGAZINE = "PRINT_MAGAZINE",
  NO_ADS = "NO_ADS",
}